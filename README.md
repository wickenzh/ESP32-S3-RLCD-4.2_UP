# ESP32-S3 RLCD Weather Clock OTA

这个仓库是设备 OTA / 上位机固件镜像仓库，不保存固件源码。

## 当前版本

- 最新版本：`v1.5.12`
- Manifest：`firmware/latest.json`
- 版本清单：`firmware/versions.json`

## 文件用途

- `firmware/latest.json`：设备端检查 OTA 时读取的最新版本清单。
- `firmware/versions.json`：上位机读取最近版本列表，包含 app bin 和 merged bin 的 URL、大小和 SHA256。
- GitHub Release 附件 `weather_clock_vX.X.X.bin`：设备 OTA 升级用 app 固件。
- GitHub Release 附件 `weather_clock_vX.X.X_merged.bin`：串口完整刷写镜像，包含 bootloader、分区表、OTA data 和 app。

## 同步来源

本仓库由 GitHub Actions 自动从 Cloudflare R2 OTA 存储同步。

- R2 是设备默认 OTA 下载源。
- GitHub Release 是备份、历史下载和上位机读取的镜像。
- 每次同步最多保留最近 `10` 个版本。

## 最近版本

- `v1.5.12`：ESP32-S3 RLCD 4.2 天气时钟 v1.5.12 源码发布。

`v1.5.12`：1. 修复冷启动天气同步在连续内存保护延期后丢失待办的问题；资源暂时不足时保留任务并错峰重试，启动压力窗口结束后自动继续补拉，天气时钟和天气看板不再长期停留在“等待数据”。
  2. 加固网络配置与业务 NVS 的失败输出和变化标记：底层完整写入成功后才报告状态变化，读取、擦除或写入失败时不再传播陈旧句柄、文本或错误的 changed 状态。
  3. 稳定优先清理主程序、音频、传感器、电源、QWeather、每日文字、配网和 Wi-Fi 生命周期中的断言专用重复文本登记与低价值编译期约束，保留真实协议、容量、时序、功耗和错误处理行为。
  4. 完成 GitHub 源码自动构建与 Cloudflare R2 导入链路收尾：版本标签触发固定 ESP-IDF 构建双固件，manifest push 通过签名 Webhook 即时触发 R2 原子导入，不再使用周期轮询或由本机重复上传大固件。
  5. 新增并扩展网络同步调度、GitHub 工作流、manifest 与 Cloudflare Webhook 主机测试；本次正式发布将端到端核验 GitHub Release 双附件及 R2 latest.json、versions.json、app、merged 四项产物。
  6. 本版不修改 UI、分区表、NVS key、资源格式、OTA 协议或上位机接口；已正确刷入 v1.5.x 分区表的设备可直接 OTA 升级，无需恢复出厂。

本 Release 仅包含公开的 RLCD_CLOCK 时钟源码；严禁商用。
  - app sha256: `9b39646030565555a2762ae86d3763d0e46c6729ffc149d89bdf5406149031b7`
  - merged sha256: `a4c5edab18af57f5f25c6153762f184115dd503a7b7c2e7fb8a1ee0010e521e6`
- `v1.5.11`：ESP32-S3 RLCD 4.2 天气时钟 v1.5.11 源码发布。

`v1.5.11`：
  1. 本版用于验证新的外网自动发布链路，只顺延固件、SDL 和工程版本号，不修改设备业务逻辑、UI、分区表、NVS、资源格式、OTA 协议或上位机接口。
  2. GitHub 公开源码仓库新增固定 ESP-IDF `v5.5.3` 自动构建：版本标签触发 app 固件与包含 bootloader、分区表、OTA data、语音模型和 app 的 merged 固件构建，并把两个附件追加到同版本 Release。
  3. 自动构建严格保留既有中文 Release 说明，只增加或覆盖两个固件附件；随后按实际构建文件重新计算 size 和 SHA256，更新公开仓库中的 `latest.json` 与最近 10 版 `versions.json`。
  4. Cloudflare Worker 改为每 15 分钟从 GitHub 主动拉取 manifest 与 Release 固件；app 和 merged 采用流式写入 R2，两个固件都通过长度、SHA256 和 R2 回读校验后才覆盖在线 manifest，避免半发布状态。
  5. GitHub Actions 不持有 Cloudflare 写入令牌；受保护的 Worker 手动同步和本机 R2 直传只保留为维护者应急入口，降低第三方运行器凭据暴露风险。
  6. 正式发布默认只需本机完成编译验证、发布 Gitea 并同步 GitHub 源码；等待脚本会确认 GitHub Release 双附件和 R2 `latest.json`、`versions.json`、app、merged 全部就绪，不再由本机重复上传大固件。
  7. 新增 manifest 生成与 Worker 同步主机测试，覆盖哈希/大小、最近 10 版、清单长度、无效输入、双固件成功导入、merged 下载失败时不更新 manifest，以及 Cloudflare 定时同步路径。

本 Release 仅包含公开的 RLCD_CLOCK 时钟源码；严禁商用。
  - app sha256: `b487ea38a4ddff2cd7c9bb491a08eb2ac2f78298e2bc65a019f05e0b3fe69863`
  - merged sha256: `2be5cc0c6bda26d125516477d58ae6f8acd79089241abb412c9c6fe1e8d70ea5`
- `v1.5.10`：`v1.5.10`：
  1. 本版继续执行“稳定优先、低风险、可验证”的代码收敛，不新增或删除功能，不改变七个工作页、设置交互、UI 坐标与像素效果、联网频率、功耗策略和用户使用方式。
  2. 收敛 OTA 内部职责：检查与安装请求、运行期显示静默/任务看门狗、HTTP 成功状态、manifest 缓存和临时下载缓冲分别维护；R2 主源、GitHub 备用源、重定向、SHA256、双 OTA 写入、进度 UI、成功提示和重启时序保持不变。
  3. 加固小智 AI 会话资源：TTS 唤醒打断、入站文本分派、激活请求体、绑定码播报、会话临时大缓冲和网络保活形成明确所有权；编解码初始化失败会即时回滚，停止超时和异常响应保留可定位日志。
  4. 完善小智音频边界：统一 AEC 采集长度、PCM 队列尺寸和 size_t/int 转换保护，保留原唤醒、连续对话、字幕、MCP、音量、闹钟与番茄钟行为，离页后的音频、网络和电源资源仍按原顺序释放。
  5. 收敛 Wi-Fi 与配网页跨任务状态：射频开关、门户活跃状态、DNS 停止/任务活动、主动停机抑制重连和最近断线原因均通过原子快照发布；状态栏 Wi-Fi 图标继续严格表示射频是否开启，配网、OTA、小智和延后关机策略不变。
  6. 加固网络与天气运行态：普通 HTTP client 使用作用域清理，默认超时、天气/NTP 成功时间、网络检测九行结果、天气/每日文字缓存和启动补拉状态通过窄接…
  - app sha256: `e0d963141b7910fa14f06f5db0eaf666a99ec62c6fd32b671501ff7e4dfb1426`
  - merged sha256: `f560e7fd69210353f6bb4b7f26cadffe806b6bf3ff9c0aef98b7fe6fb88ebf2f`
- `v1.5.9`：`v1.5.9`：
  1. 继续按稳定优先原则拆分启动联网、后台同步和可见页数据补拉职责；天气与每日文字仍由单一常驻网络任务串行错峰，页面 UI 不直接执行 HTTP/HTTPS。
  2. 加固冷启动资源分配：启动动画阶段不再执行页面 HTTPS，天气、预警、预报、空气质量和每日文字在启动后错峰补齐，并在开机前一分钟持续检查 internal/DMA 连续内存，降低 TLS、JSON、RLCD 同时占用造成的峰值压力。
  3. 统一 HTTPS/WSS 资源保护：天气、每日文字、小智激活和实时会话共用显示 DMA 保守模式、内存快照和全局 HTTP 事务锁；OTA 大文件下载也纳入同一事务互斥，避免多个 TLS 会话并存。
  4. 收敛 Wi-Fi 与配网生命周期：初始化按 netif、driver、事件注册和 APSTA 配置分阶段执行，失败时完整逆序回滚；运行期配网与冷启动配网复用 SoftAP 配置、连接返回码处理和明确失败日志。
  5. 拆分天气与 QWeather 基础职责：动态响应缓冲使用作用域释放，天气定位、城市查询、实时天气、预警、预报和空气质量继续保持原请求顺序、旧缓存保留和整批提交语义。
  6. 拆分 OTA 清单客户端、固件流下载和进度上报职责；来源顺序、重定向、SHA256、双 OTA 写入、R2 主源、GitHub 备用源、下载 UI、确认超时、成功提示和重启时序保持不变，并补齐关键失败阶段日志。
  7. 加固小智 AI 网络与协议边界：激活客户端、WebSocket…
  - app sha256: `cd21709fd9ac0ea232563b56f0f7db9093a59ccc40824d568d2c22dcb7b9f942`
  - merged sha256: `4411a1aebb13423a2b0f5cbeca6cdd87825ecf8a0ea6d654a0857d44dbd3bde3`
- `v1.5.8`：`v1.5.8`：
  1. 继续按稳定优先原则精细化主程序结构，拆分启动联网、后台同步、天气批量更新、QWeather URL/城市/实时天气/预警解析等职责；接口调用顺序、缓存提交、失败退避和 Wi-Fi 生命周期保持不变。
  2. 收敛联网配置与 NVS 维护边界：页面、整点提醒、闹钟、天气城市、恢复出厂和小智激活存储分别维护，继续使用原 namespace、key、值类型、提交条件和恢复规则。
  3. 加固小智 AI 可维护性：拆分激活客户端、绑定码播报、服务端 hello、入站事件、MCP 参数/请求信封/JSON 和 WebSocket 文本构建；现有上游基线、协议字段、工具顺序、绑定信息和用户交互保持不变。
  4. 统一小智会话状态收尾：自动返回、语音前端停止和 TTS 回合计时复位使用单一窄入口，保留 5 分钟返回、番茄钟/对话保护、尾帧排空、告别回复和恢复聆听规则。
  5. 继续收敛 UI 主循环和工作页共享逻辑：页面名称、低刷新分类、状态栏标签/温湿度映射、对象可见性、设置页同步超时和退出清理集中维护，隐藏页不增加额外刷新。
  6. 清理多余 UI 状态与重复写入：移除永久隐藏的旧开关文字对象、System Info 遗留 OTA 引用和天气看板重复显隐写入；设置页 OTA 进度、菜单 label、声音保存回滚及手动同步请求改为复用既有路径。
  7. 收窄天气时钟、图片时钟、天气看板、温湿时钟、日历、温湿历史、小智…
  - app sha256: `14755a137be58036980855fde568fdb9f8210787243fca82f44cbcd7e3d47da3`
  - merged sha256: `24e7772b77e69e7738613eb0f4531bbb5c4d538a217f259e3669120fcb9b8e3e`
- `v1.5.7`：`v1.5.7`：
  1. 统一未充电电池 ADC 与本地温湿度的日夜采样节奏：白天每个整分钟、夜间每 2 分钟整边界采样；开机首样、时间不可信 60 秒兜底和确认充电后的约 1 秒快速采样保持不变。
  2. 修复并加固小智 AI 音频链路：避免 TTS 回复被 WakeNet 回声误中断，增加唤醒音静音预热和外设稳定等待，并在 Codec 关闭异常时保留可恢复状态，降低回复中途停止和偶发沙哑风险。
  3. 继续收敛七页显示刷新：全天进度条共用单一模块，日历星期条与日期区独立定位，反显数字牌形成共享绘制组件；隐藏页、低频页和秒级页仍按原可见性及局部刷新规则运行。
  4. 加固 FreeRTOS Tick 长期运行边界：UI 短截止时间、传感器 housekeeping、可见页同步退避、OTA 状态保持、小智自动返回和音频等待统一使用回绕安全判断，避免长时间运行后出现误超时或高频空转。
  5. 拆分天气组合更新职责：手动城市、IP 定位回退、实时天气、预警、未来预报和空气质量保持原请求顺序与缓存提交语义，endpoint 客户端与批量业务编排分别维护。
  6. 加固网络输入处理：JSON 根对象、字段复制、URL component 编码和 gzip payload 解析形成独立模块，补充损坏数据、超长字段和边界输入主机测试，天气 API、每日文字和配网表单行为不变。
  7. 拆分并测试 OTA manifest 解析，修复超长版本组件比较可能溢出的边界；R2 主…
  - app sha256: `d76eb178e18593df745684ab8c86e34e28d2f7e9fa23a4bc15960dc9380459fc`
  - merged sha256: `2b56436e0bda60380dc9d5098bcf3a97acc8f07fe897611cfc7872d9fed26e3b`
- `v1.5.6`：`v1.5.6`：1. 统一七个工作页的全天进度条与可见页刷新边界，进度仅在跨越约 24 分钟分段时局部更新；日历主体位置同步微调，页面布局和交互保持不变。\n2. 修复番茄钟倒计时偶发视觉跳秒，并为覆盖已有闹钟增加二次确认；闹钟、番茄钟与小智自动返回的既有互斥和后台计时规则保持不变。\n3. 加固充电与电池显示：提取充电趋势状态机、统一整秒闪烁判定、只刷新当前可见页电池对象，并修复长期运行 Tick 回绕附近的状态异常风险。\n4. 加固天气与每日文字按需同步：统一可见页重试状态机、缓存过期规则和失败退避，并修复系统长期运行后 Tick 回绕可能导致补拉失效的问题。\n5. 加固 OTA 时序与校验：统一清单来源、版本、SHA256、下载展示和状态保持规则，保留 R2 主源与 GitHub 备用源协议、双 OTA 分区及回滚行为。\n6. 收敛启动与传感器调度：启动联网预算、温湿度/电池采样时序、四小时趋势及小时历史职责分离，保持原采样频率、NVS 格式和页面数据不变。\n7. 拆分联网配置职责：配网页字段解析、在线/离线提交、业务 NVS、EventGroup 安全适配和通用 NVS 原语分别维护，补齐半初始化和异常输入回归。\n8. 拆分网络与天气基础模块：强制门户 DNS/DHCP、QWeather 响应、定位、预警、预报、空气质量、共享天气快照和每日文字解析形成清晰边界，保持 API、缓存与 UI 结…
  - app sha256: `142ecd89057125b531a08b597f88acb303cdf8db77caf43bbcf9674d0846f9a6`
  - merged sha256: `b93e0fd05647cf0574310118e8b5ec71657f238e79d2459132aadc6882901aea`
- `v1.5.5`：`v1.5.5`：
  1. 修复显示设置“页面开关”索引误归零：日历、温湿历史和小智 AI 现在会正确切换自身启用状态，不再把光标和操作跳回天气时钟。
  2. 修复小智“退下/退下吧”退出话术：设备会进入既有的告别语收尾流程，完整播放回复后返回主页，不再把服务端正常结束误报为语音会话中断。
  3. 调整图片时钟内置图库：替换为星期日到星期六七张新素材，固定阈值 211、关闭抖动和额外边缘处理；内置图按当前星期每天切换，自定义图库仍优先显示并按连续日期每天轮换。
  4. 修复设备完全断电、RTC 时间丢失后的启动数据显示问题：开机先完成本地温湿度采样，时间无效时优先同步 NTP，再错峰获取天气和每日文字，避免进入工作页后长时间没有时间或温湿度。
  5. 完善无网络兜底显示：即使 RTC 时间仍不可信，非时钟页面也会显示本地温湿度，时间相关区域使用占位状态，联网校时成功后自动恢复正常显示。
  6. 修复旧固件检查更新时可能无法解析新版本清单的问题：OTA manifest 增加 1800 字节上限，超长更新说明只在设备清单内安全截短，README、Gitea 和 GitHub Release 仍保留完整说明。
  7. 完成手机端配网页中文化：Wi-Fi、密码、QWeather API Key、天气城市、离线时间和状态提示统一使用中文，表单提交、NVS 保存和配网流程保持不变。
  8. 收敛天气与每日文字状态：…
  - app sha256: `dc6752c86808476d9b99a537b098bc99d69499e5fd90184f8ea4a8a718f57553`
  - merged sha256: `b1a9535cfd197ecb1a714cde91e6ada597c4c53e9106a9cb6951f13fa644b304`
- `v1.5.4`：`v1.5.4`：
  1. 完成稳定优先的模块精细化拆分：设置反馈、设置导航、OTA 面板、UI 对象引用、工作页目录、LVGL 通用控件、日期时间格式化、DSEG 数字、packed 位图、状态 GIF、共享进度条和页面过渡均形成独立职责，页面坐标、刷新频率和按键行为保持不变。
  2. 拆分并加固天气与配网链路：QWeather 图标、预警文本、每日预报、定位文本和响应生命周期独立管理；配网页字段解析、强制门户 DNS、页面渲染及第三方 IP 定位请求分离，修复配网停止后的任务句柄残留。
  3. 补充底层稳定性保护：恢复出厂键表增加唯一性约束，自定义资源初始化按阶段校验，温湿历史加载诊断更明确；HTTP 请求头、OTA 请求头、Wi-Fi 首次连接、小智激活和 WebSocket 选项失败均保留可定位日志与安全回退。
  4. 收敛网络、电池和 OTA 生命周期：网络同步唤醒锁按作用域释放，电池充电趋势状态集中管理，OTA 版本与 SHA256 校验拆为纯逻辑模块；不改变双 OTA 分区、R2/GitHub 主备下载协议或既有回滚策略。
  5. 完善显示设置规则：新增可选“小智 AI 自动返回”，开启后小智页连续 5 分钟无活动返回主页；小智不能成为排序第一的主页，也不能成为唯一启用页面。温湿历史解除旧的 5 分钟自动返回限制，与普通页面一致。
  6. 接入受限小智 MCP 并记录上游基线：支持查询本地温湿度、电池和音量、设置…
  - app sha256: `042c265e9cdcdf7791f3bdfd91a22f6ef853566afbed7ea236c403bfad58bd48`
  - merged sha256: `b556a10851c820fba021e6521595e8d599997f159b3b8578349b55ff981d1f10`
- `v1.5.3`：`v1.5.3`：
  1. 收紧天气 JSON 只读契约：QWeather 城市、实时天气、预警、预报和空气质量解析统一使用只读节点，防止后续维护意外修改 HTTP 响应树；字段、缓存和显示行为不变。
  2. 统一 NVS 条件提交路径：离线模式、手动城市、声音提醒、页面启用、页面排序和恢复出厂键清理统一遵循“内容变化才提交”，保留底层错误传播并减少重复 Flash 写入判断。
  3. 统一 UI 辅助页阻断判断：设置页、关于本机和网络检测页在低刷新等待、温湿时钟轮询、小智生命周期、低电量及充电动画路径中复用同一判断，降低状态遗漏风险。
  4. 拆分 RLCD 显示刷新职责：将 LVGL flush、局部范围合并、全屏阈值、OTA 静默和刷新诊断从页面调度迁入独立模块，迁移前后刷新算法和参数保持一致。
  5. 拆分共享电池 UI：五格电池图标、分段算法和充电闪烁绘制迁入独立模块，六个稳定页面及其它调用方改为显式依赖，坐标、样式和动画规则不变。
  6. 拆分配网状态面板：天气时钟内嵌的 AP、Portal 和 STA 六行状态创建与刷新集中到独立模块，清理设置页中的重复常量和跨模块职责，不改变配网流程。
  7. 完成 `git diff --check`、代码文件头检查、ESP-IDF 与 SDL 构建；本版不修改分区表、OTA 协议、页面布局、数据格式和用户操作流程，可从 `v1.5.2` 直接 OTA 升级。
  - app sha256: `c0ad010004b69e40d0b912b81b8d560966d611d824cc70e9671956b5a84fd741`
  - merged sha256: `556a2b51ef58ce730c2cd07dc40dbf861088d3029090ae3020a0357a030b641f`
